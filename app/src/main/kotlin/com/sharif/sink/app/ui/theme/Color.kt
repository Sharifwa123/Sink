package com.sharif.sink.app.ui.theme

import androidx.compose.ui.graphics.Color

// Sink brand: deep ink navy + a single signal/mesh accent, used sparingly.
// Deliberately not "AI-purple" or gradient-heavy — this is meant to read as a
// serious networking/communications tool.
val SinkInk = Color(0xFF0B1220)
val SinkInkLight = Color(0xFFF7F9FC)
val SinkSignal = Color(0xFF2DD4BF)
val SinkSignalDark = Color(0xFF0F766E)
val SinkSlate = Color(0xFF64748B)
val SinkError = Color(0xFFDC2626)
